*** Settings ***
Default Tags
Resource          ../Config/config.txt

*** variable ***
${version}    2    # 固定

*** Test Cases ***
测试服获取install_token
    [Tags]    smoke
    测试服获取install_token    &{user_id}[user2]

smoke
    [Tags]    smoke
    ${response}=    找朋友未读消息数
    校验接口返回状态正常    ${response}
