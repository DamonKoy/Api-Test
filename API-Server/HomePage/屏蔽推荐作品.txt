*** Settings ***
Default Tags
Resource          ../Config/config.txt

*** variable ***
${page}    1
${pagesize}    30

*** Test Cases ***
测试服获取install_token
    [Tags]    smoke
    测试服获取install_token    &{user_id}[user2]

smoke
    [Tags]    smoke
    ${response}=    屏蔽推荐作品    ${box_id}    ${box_type}    ${reason_tag}
    校验接口返回状态正常    ${response}

校验返回值
    ${response}=    屏蔽推荐作品    ${box_id}    ${box_type}    ${reason_tag}
    校验接口返回状态正常    ${response}
