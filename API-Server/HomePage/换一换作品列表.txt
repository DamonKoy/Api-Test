*** Settings ***
Default Tags
Resource          ../Config/config.txt

*** variable ***
${pagesize}    30

*** Test Cases ***
测试服获取install_token
    [Tags]    smoke
    测试服获取install_token    &{user_id}[user2]

smoke
    [Tags]    smoke
    ${type}    set variable    1
    ${response}=    换一换作品列表    ${type}    ${pagesize}
    校验接口返回状态正常    ${response}

作者列表
    [Tags]    smoke
    ${type}    set variable    2
    ${response}=    换一换作品列表    ${type}    ${pagesize}
    校验接口返回状态正常    ${response}

校验返回值
    ${response}=    换一换作品列表    ${type}    ${pagesize}
    校验接口返回状态正常    ${response}
