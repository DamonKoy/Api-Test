*** Settings ***
Default Tags
Resource          ../Config/config.txt

*** variable ***
${pagesize}    6    # app默认为6

*** Test Cases ***
测试服获取install_token
    [Tags]    smoke
    测试服获取install_token    &{user_id}[user2]

smoke
    [Tags]    smoke
    ${response}=    换一换首页推荐频道列表    ${pagesize}
    校验接口返回状态正常    ${response}

校验返回值
    ${response}=    换一换首页推荐频道列表    ${pagesize}
    校验接口返回状态正常    ${response}

校验返回值list数组长度
    ${response}=    换一换首页推荐频道列表    ${pagesize}
    校验接口返回状态正常    ${response}
    ${length} =    Get Length    ${response.json()['data']['list']}
    Should Be true    ${length} >= 0 and ${length} <= 6
