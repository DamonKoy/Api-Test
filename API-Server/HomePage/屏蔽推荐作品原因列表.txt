*** Settings ***
Default Tags
Resource          ../Config/config.txt

*** variable ***


*** Test Cases ***
测试服获取install_token
    [Tags]    smoke
    测试服获取install_token    &{user_id}[user2]

smoke
    [Tags]    smoke
    ${response}=    屏蔽推荐作品原因列表    ${box_id}    ${box_type}
    校验接口返回状态正常    ${response}

校验返回值
    ${response}=    屏蔽推荐作品原因列表    ${box_id}    ${box_type}
    校验接口返回状态正常    ${response}
