*** Settings ***
Default Tags
Resource          ../Config/config.txt

*** variable ***
${page}    1
${type}    1
${version}    1

*** Test Cases ***
测试服获取install_token
    [Tags]    smoke
    测试服获取install_token    &{user_id}[user2]

smoke
    [Tags]    smoke
    ${response}=    漫画社消息    ${page}    ${type}    ${version}
    校验接口返回状态正常    ${response}
